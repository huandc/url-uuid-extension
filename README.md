# url-uuid-extension

从当前页面 URL 中提取 UUID 的 Chrome 插件（Manifest V3），支持一键复制与替换后刷新。

插件代码位于 [`modules/chrome-uuid-extension/`](modules/chrome-uuid-extension/)，使用说明见其 [README](modules/chrome-uuid-extension/README.md)。
